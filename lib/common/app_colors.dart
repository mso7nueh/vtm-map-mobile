import 'package:flutter/material.dart';

const Gradient accent = LinearGradient(
  begin: Alignment.center,
  end: Alignment.centerRight,
  colors: [
    Color(0xFF0037FF),
    Color(0xFF0085FF),
  ],
);
